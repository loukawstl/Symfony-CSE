{% block javascripts %}
    <script type="text/javascript" src="{{ asset('js/Menu.js') }}"></script>
{% endblock javascripts %}
    <div class="menu">
        <div class="align-text-bottom flex center navbar z-10 text-white text-center">
            <div class="ml-52 " >
                <img src="{{ asset('assets/Logo-Saint-Vincent.png') }}">
            </div>
            <a class="px-5 mt-14 pt-3 active:bg-topHover hover:bg-topHover" href=" {{ path('app_home_page') }}">Accueil</a>
            <a class="px-5 mt-14 pt-3 active:bg-topHover hover:bg-topHover"href="{{ path('app_partnership') }}">Partenariats</a>
            <a class="px-5 mt-14 pt-3 active:bg-topHover hover:bg-topHover"href="{{ path('app_a_propos') }}">A propos de nous</a>
            <a class="px-5 mt-14 pt-3 active:bg-topHover hover:bg-topHover"href="{{ path('app_ticketing') }}">Billetterie</a>
            <a class="px-5 mt-14 pt-3 active:bg-topHover hover:bg-topHover"href="{{ path('app_contact') }}">Contact</a>
        </div>
    </div>

